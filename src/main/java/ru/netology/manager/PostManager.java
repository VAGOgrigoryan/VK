package ru.netology.manager;

import ru.netology.domain.Post;

public class PostManager {

    private Post[]posts;

    public Post [] searchPost(int ownerId, String domain, String query, int count, int offset){ //Позволяет искать записи на стене в соответствии с заданными критериями.

        return posts;
    }
    public void deletePost (int ownerId, int postId){ //Удаляет запись со стены.
        return;
    }

}